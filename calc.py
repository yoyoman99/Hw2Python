def calculator(str):
    result = 0
    arr = list(str)
    stack = []
    for i in range(len(arr)):
        if arr[i] == "+" or "-":
            stack.append()
        elif arr[i] == "*" or "/" or "//":
            stack.append()
        else:
            stack.append()
    return arr
